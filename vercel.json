{
  "version": 2,
  "installCommand": "cd frontend && npm install --include=dev",
  "buildCommand": "cd frontend && npm install --include=dev && npm install --no-save @rollup/rollup-linux-x64-gnu @esbuild/linux-x64 && npm run build",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
